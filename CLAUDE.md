@AGENTS.md

## Claude Code only
- Use plan mode before changing turn resolution in `sim/` or anything in `contracts/`.
- When running locally alongside Codex, work in your own git worktree. Never share a working directory with another agent.
